;; Cycle between window setups with C-c <left> / C-c <right>
(winner-mode)

;; TODO: Need to make this work with Org mode because the modifier is SHIFT
(windmove-default-keybindings)

(provide 'init-windows)
